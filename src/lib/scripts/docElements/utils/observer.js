export default function observer(root){
   /* const ROOT_ELEMENT = root;
    let mutationObserver;

    return {
        create,
    }

    function create(callback, config){
        mutationObserver = new MutationObserver(callback);

        mutationObserver.observe(ROOT_ELEMENT, config);
    }

    function callback(){

    }*/
}